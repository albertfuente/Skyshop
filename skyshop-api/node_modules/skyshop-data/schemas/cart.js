/* const { Schema } = require('mongoose')

module.exports = new Schema({
    productId: {
        type: Number,
        required: true
    },
    quantity: {
        type:Number,
        required:true
    } 
}) */